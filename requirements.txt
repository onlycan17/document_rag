# Core Framework
streamlit==1.32.2

# LangChain and Extensions
langchain==0.3.14
langchain-community==0.3.14
langchain-openai==0.3.1
langchain-google-genai==2.0.7
langchain-anthropic==0.3.0
transformers>=4.53.0
tokenizers>=0.21,<0.22
langchain-chroma==0.1.4
# 주의: langchain-upstage는 tokenizers<0.21을 요구하여 최신 transformers(tokenizers>=0.21)와 충돌합니다.
# 설치 스크립트에서 `--no-deps`로 별도 설치합니다. (requirements에서 제외)

# Vector Databases
faiss-cpu==1.9.0.post1
chromadb==0.4.24

# Embeddings and ML
sentence-transformers==3.3.0
torch>=2.0.0  # sentence-transformers(토큰 분할기)의 런타임 의존성

# PDF Processing
pypdf>=4.2.0,<5.0.0
PyPDF2==3.0.1
pdf2image==1.17.0  # pdf2image에는 'jpeg' extra가 없습니다
pytesseract==0.3.13
PyMuPDF==1.25.2  # For advanced PDF processing and image extraction

# DOCX Processing
python-docx==1.1.2

# Image Processing
pillow>=7.1.0,<11
opencv-python==4.10.0.84  # For advanced image analysis and classification

# API Clients
google-generativeai==0.8.3
anthropic==0.42.0
openai>=1.0.0  # For OpenAI embeddings support

# Configuration and Environment
python-dotenv==1.0.1
pydantic==2.10.0
pydantic-settings==2.7.0

# Utilities
tiktoken==0.8.0
numpy>=1.24.0
requests>=2.28.0

# Protocol Buffers (for various dependencies)
protobuf==4.25.8

# 내장(로컬) 모델 지원 제거: llama-cpp-python·timm·bitsandbytes 등 삭제 (외부 API 전용 전환)

# Note: System dependencies (installed via apt/brew):
# - tesseract-ocr (for OCR functionality)
# - poppler-utils (for PDF to image conversion)
# These need to be installed separately using install_ocr.sh
# LLM 관측(Langfuse): 선택 설치 — pip install langfuse==4.15.2
#   (오픈텔레메트리 의존성 해석이 무거워 기본 requirements에서 제외.
#    미설치 시 트레이싱만 비활성화되고 앱 동작에는 영향 없음. docs/LANGFUSE_GUIDE.md 참조)
