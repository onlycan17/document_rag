# TODO / TASK 추적

규칙: 대분류 → 중분류 → 소분류. 완료 시 체크. 문서/코드 변경 시 항상 동기화.

## 현재 작업
- [ ] 문서 1차 생성 후 코드와 상이한 부분 보정
- [x] RAG 응답 구조화 레이어(AnswerFormatter) 설계 및 구현 — `src/utils/answer_formatter.py` 구현 완료, query_engine 표준/스트리밍 경로 연동(2026-09-10)

## 대분류: 데이터 파이프라인
- [ ] 로더
  - [ ] PDF OCR 경로에서 품질 낮은 페이지 감지/재시도 로직 강화
  - [ ] Markdown 표/코드블록 처리 규칙 명시화
- [ ] 청크/전처리
  - [ ] 긴 표/목차 전용 분할 규칙 추가
  - [ ] 문서별 커스텀 분할 프로필 지원

## 대분류: 검색 품질
- [ ] 하이브리드
  - [ ] BM25/TF‑IDF 가중치 동적 최적화(피드백 기반)
  - [x] 유사 질의 캐시 — `VectorDatabase.search`에 TTL 캐시(전처리 완료 쿼리 정확 일치, `QUERY_CACHE_TTL_SECONDS`=300초/`QUERY_CACHE_MAX_ENTRIES`=64), `add_documents`·`clear_database`에서 무효화, 테스트 6건(2026-09-10)
- [ ] 임계값
  - [ ] 문서 클래스별 임계값(규정/FAQ/보도자료 등) 차등 적용
  - [ ] 결과 부족 시 재검색 전략 카탈로그화

## 대분류: RAG 체인
- [ ] 대용량 컨텍스트
  - [x] 요약 길이 자동 튜닝 — `resolve_summary_length`(출력 토큰 한도 기반, 한글 1토큰≈1문자 보수 가정) 순수 함수 + rag_chain 초기화·update_llm 연동, 테스트 3건(2026-09-10)
  - [ ] 병렬 워커 동적 스케일
- [ ] 프롬프트
  - [x] 출처 인라인 인용 템플릿 추가 — 프롬프트에 `[출처 n]` 표기 규칙, 컨텍스트 `[문서 n]`과 출처 번호 정합화(prepare_documents), 참고 자료 블록 `- [출처 n]` 형식(2026-09-10)
  - [ ] 용어 풀이 자동화(초등학생 설명 규칙 강화)
- [ ] 응답 후처리
  - [x] AnswerFormatter로 `핵심 요약/상세 설명/참고 자료` 자동 정렬 — 구현·연동 완료, 인용 번호 정합화 테스트 추가(2026-09-10)
  - [ ] 스트리밍 응답 중간 단계에서도 구조를 유지하도록 UI 연동

## 대분류: UI/UX
- [x] 출처 하이라이트 및 문단 점프 — `src/utils/source_highlight.py`(질문 키워드 추출·문단 점프 미리보기, 테스트 9건) + 채팅 UI "📑 출처 보기" expander(문서별 관련 문단 하이라이트 + 전체 내용), 대화 기록 재렌더링 포함, AppTest 무예외(2026-09-10)
- [ ] 다크 모드
- [ ] 사이드바 파라미터 설명 툴팁 정비

## 대분류: 테스트/품질
- [ ] E2E: “문서→검색→응답” 최소 1경로 자동 테스트
- [ ] 임베딩 재시도/429 백오프 단위테스트
- [ ] 검색 임계값/하이브리드 회귀테스트(샘플 데이터셋)
- [ ] 린트/포맷팅 표준화(Ruff/Black/Mypy) 도입 제안

## 대분류: 운영/보안
- [ ] 키 미설정/잘못된 키 시 UX 개선 가이드 배너
- [x] 로그 민감정보 마스킹(용어(설명: 개인/비밀 데이터는 별표로 가리기)) — `SecretMaskingFormatter`(환경변수 키 값 + sk-/AIza/Bearer 패턴 마스킹)를 `setup_logging` 전 핸들러에 적용, 테스트 6건(2026-09-10)
- [ ] 인덱스 백업/복구 가이드 정리

## 완료
- [x] PRD/아키텍처/UI/기능/스택/API/ERD/테이블 명세 1차 작성
- [x] 전처리 기본 모델 저비용·멀티모달 지향 업데이트(openai/google/anthropic)
- [x] 멀티모달 전처리 기본 ON
- [x] 멀티모달 모델 목록 최신화 및 .env 기반 확장(EXTRA_MULTIMODAL_*)
- [x] OpenRouter 이미지 분석 기본 경로(z-ai/glm-4.5v) 도입
- [x] OpenAI Responses API 우선 폴백 로직 추가
- [x] UI 임포트 누락 보완(PreprocessingModelFactory)

## 다음 단계
- [ ] PRD 지표 측정 로깅 포인트 정의
- [ ] 샘플 데이터셋 기준 품질 벤치마크 작성
